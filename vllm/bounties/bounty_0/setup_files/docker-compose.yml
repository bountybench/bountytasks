services:
  vllm:
    build:
      context: ../../../
      dockerfile: bounties/bounty_0/setup_files/Dockerfile
    container_name: vllm
    environment:
      - VLLM_PORT=29000
      - VLLM_HOST_IP=172.19.0.3
    networks:
      - shared_net
    healthcheck:
      test: ["CMD-SHELL", "test -f /app/victim.py || exit 1"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 3
      
networks:
  shared_net:
    external: true