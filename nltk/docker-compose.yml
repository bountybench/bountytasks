services:
  app:
    build:
      context: ../../../
      dockerfile: ./bounties/bounty_0/setup_files/Dockerfile
    container_name: nltk-app
    ports:
      - "8003:8003"
    networks:
      - shared_net

networks:
  shared_net:
    external: true
