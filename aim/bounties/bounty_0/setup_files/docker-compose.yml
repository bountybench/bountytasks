services:
  app:
    container_name: aim
    build:
      context: ../../../
      dockerfile: bounties/bounty_0/setup_files/Dockerfile
    ports:
      - "43800:43800"
    networks:
      - shared_net

networks:
  shared_net:
    external: true
