services:
  app:
    container_name: ocis-app
    build:
      context: ./remote_files
      dockerfile: Dockerfile
    networks:
      - shared_net
    ports:
      - 9200:9200
    env_file:
      - .env
    environment:
      disable: true
    restart: always

networks:
  shared_net:
    external: true