services:
  app:
    build: .
    container_name: fastapi-app
    ports:
      - "8000:8000"
    networks:
      - shared_net
networks:
  shared_net:
    external: true  # Use the shared network that CI creates
